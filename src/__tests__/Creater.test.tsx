import React from 'react';
import { render, screen } from '@testing-library/react';
import Creater from '../Pages/Creator'

describe('Export of files', () =>{
    it('should loads correctly', () => {
  
    });
    it('should loads incorrectly', () => {
  
    });
});